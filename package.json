{
  "name": "ay-loadingscreen",
  "version": "1.0.0",
  "private": true,
  "description": "AY Roleplay FiveM loading screen — build tooling for the production (obfuscated/minified) release.",
  "scripts": {
    "build": "bash build.sh"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.7.0"
  }
}
